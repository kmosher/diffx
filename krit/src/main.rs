//! krit — the v2 backend. See docs/design/krit-v2.md for the architecture
//! and build order. Slice 1 (walking skeleton): embedded UI + /api/diff +
//! /api/state, wire-compatible with the frozen v1 API contract.

fn main() {
    println!("krit 2.0.0-alpha.0 — walking skeleton not yet built; see docs/design/krit-v2.md");
}
